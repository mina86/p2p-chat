                                                                -*- org -*-

* Do wciêæ stosujemy tabulatory o szeroko¶ci 4 znaków.  Do
  wyrównywania tekstu z poprzedni± linijk± stosujemy natomiast spacje,
  np.:

    if (warunek) {
    TAB>funkcja(dluga, lista,
    TAB>        argumentow);
    }

** Staramy siê nie robiæ wiêcej ni¿ 3 poziomów wciêæ.  Je¿eli
   gdzie¶ jest du¿y fragment kodu z poziomem zag³êbienia równym 4 to
   nale¿y rozwa¿yæ, czy aby takiego fragmentu nie wstawiæ do
   oddzielnej funkcji.  Wiêcej poziomów wciêæ nie ma w ogóle prawa
   zaistnieæ.

** Deklaracji po s³owie namespace nie wcinamy.

* Linie nie powinny mieæ wiêcej ni¿ 78 znaków przy za³ó¿eniu, ¿e
  znak tabulacji ma szeroko¶æ 8 znaków.

* Przy ka¿dej instrukcji z³o¿onej (if, for, while, do) stosujemy
  klamry nawet je¿eli w jej wnêtrzu jest pojedyncza instrukcja, np.:

    if (warunek) {
        instrukcja;
    }

** Jedynym wyj±tkiem jest sytuacja, gdy instrukcja z³o¿ona jest wciêta
   3 tabulatorami i nie chcemy robiæ 4 poziomu wciêæ.  W takiej
   sytuacji mo¿na zastosowaæ:

     if (wrunek) instrukcja;

  pod warunkeim, ¿e nie przekraczamy 78. kolumny.

* Nie stosujemy makra NULL, a to dlatego, ¿e wprowadza ono ludzi w b³±d
  sugeruj±c, ¿e mamy do czynienia ze wska¼nikiem.  W rzecziwysto¶ci NULL
  mo¿e byæ litera³em 0 (w szczgólno¶ci w C++) zatem je¿eli mamy funkcjê
  przeci±¿on±, z czego jedna z wersji przyjmuje int, a druga wska¼nik to
  wywo³anie jej z argumentem NULL mo¿e z du¿ym prawdopodobieñstwem
  uruchomiæ t± przyjmuj±c± inta, a nie wska¼nik.

* Program kompilujemy z u¿yciem nastêpuj±cych prze³±czników:

  -Wall -Wextra -Wfloat-equal -Wshadow -Wwrite-strings -Winline
  -Wdisabled-optimization -Wstrict-aliasing=2 -pedantic
  -Wpointer-arith -Wstrict-null-sentinel -Wctor-dtor-privacy
  -Woverloaded-virtual -ggdb -O0 -std=c++98

  i je¿eli zosta³y wygenerowane jakie¶ ostrze¿enia to poprawiamy kod.

  W niektórych przypadkach (szczególnie kod C++) mo¿na pomin±æ
  -Winline.

* Je¿eli w instrukcji switch przechodzimy z jednego case'a do drugiego
  stosujemy komentarz "FALL THROUGH", np.:

    switch (foo) {
    case bar:
        instrukcja;
        /* FALL THROUGH */

    case baz:
        instrukcja;
    }

    Brak takiego komentarza oraz brak instrukcji break nale¿y
    traktowaæ jak b³±d.

* Staramy siê nie u¿ywaæ s³ówka using.  Wszystkie identyfikatory
  podajemy z pe³nymi modyfikatorami zasiêgu (czy jak to siê tam zwie),
  np. std::string.
